<h1> </h1>
  <div class="image_daron">
  </div>

  <div class="daron_new_form">
    <div class="form_center">
    <div class="segment">
        <%= simple_form_for [@daron] do |f| %>
          <%= f.input :name %>
          <%= f.input :profile_picture %>
          <%= f.input :price %>
          <%= f.input :bio %>
          <div class="unit">
          <%= f.submit "Submit new daron", class: "btn btn-gradient" %>
          </div>
        <% end %>
    </div>
  </div>
