
  <div class= typewriter_daron_new>
    <p>N'attends plus ! poses ta Rent ci-dessous</p>
  </div>

  <div class="image_daron">
  </div>

  <div class="container_form">
    <div class="form_center">
        <%= simple_form_for [@daron] do |f| %>
          <%= f.input :name %>
          <%= f.input :profile_picture %>
          <%= f.input :price %>
          <%= f.input :bio %>
          <%= f.input :badges %>
          <%= f.input :zip_code %>
          <%= f.input :phone_number %>
          <%= f.submit "Submit for a new Daron", class: "btn btn-danger" %>

</div>
        <% end %>
    </div>
  </div>

  <%= link_to "Back Home", root_path %>
